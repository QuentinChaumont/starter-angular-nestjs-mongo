# Nx injects the repo-root .env into every task, and Angular's dev-server
# obeys process.env.PORT. This project-level .env wins over the root one
# (Nx loads project env files first, no-override), pinning the SPA to 4200.
PORT=4200
