{
  "name": "@org/backend-mailer",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "nx": {
    "name": "backend-mailer",
    "tags": [
      "scope:backend",
      "type:util"
    ],
    "targets": {
      "test": {
        "options": {
          "passWithNoTests": true
        }
      }
    }
  },
  "dependencies": {
    "@nestjs/common": "^11.2.3",
    "@org/backend-core": "0.0.1"
  },
  "//": "SMTP delivery needs `nodemailer` — install it yourself (`npm add nodemailer`) when you set SMTP_URL. It is deliberately not a dependency here so the brick, and `npm ci`, stay lean without it.",
  "devDependencies": {
    "@nestjs/config": "^4.0.4",
    "@nestjs/core": "^11.2.3",
    "@nestjs/testing": "^11.0.0"
  }
}
