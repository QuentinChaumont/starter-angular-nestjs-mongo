{
  "$schema": "http://json-schema.org/schema",
  "$id": "FrontendAuth",
  "title": "Install the frontend auth brick (login, OIDC, interceptors, guards)",
  "type": "object",
  "properties": {
    "profile": {
      "type": "boolean",
      "description": "Also add the /app/profile page (needs frontend-dashboard + frontend-feedback).",
      "default": false
    }
  }
}
