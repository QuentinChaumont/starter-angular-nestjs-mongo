{
  "$schema": "http://json-schema.org/schema",
  "$id": "FrontendFeature",
  "title": "Generate a lazy-loaded frontend feature module under /app",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Feature name (lowercase letters, digits and dashes, e.g. \"reports\").",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What name would you like to use for the feature?"
    },
    "crud": {
      "type": "boolean",
      "description": "Also generate an editable form page + create/edit routes.",
      "default": false
    },
    "roles": {
      "type": "string",
      "description": "Comma-separated roles that may open the feature (adds roleGuard + nav filter)."
    },
    "icon": {
      "type": "string",
      "description": "mat-icon ligature for the sidenav entry.",
      "default": "chevron_right"
    }
  },
  "required": ["name"]
}
