# frontend-features-<%= fileName %>

Lazy-loaded business feature, generated by
`nx g @org/starter-plugin:frontend-feature <%= fileName %>`.

- Mounted at **`/app/<%= fileName %>`** via `loadChildren` (its own bundle
  chunk — never imported statically).
- `<%= constantName %>_ROUTES` — the lazy route group (list<% if (crud) { %> / new / edit<% } %> / detail).
- `<%= className %>Service` — typed HTTP on `${API_BASE_URL}/<%= fileName %>`.
- `<%= className %>Store` — signal state for the pages.
- Sidenav entry added to `DASHBOARD_NAV`.

Replace the placeholder `name` field everywhere with the resource's real
shape — or run
`nx g @org/starter-plugin:entity <%= fileName %> --frontend` to generate a
matching backend entity + shared contract and have `<%= fileName %>.model.ts`
re-export it.
