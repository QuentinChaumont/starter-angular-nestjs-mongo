import { Injectable } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { BaseRepository } from '@org/backend-database-mongo';
import { Model } from 'mongoose';
import { <%= className %> } from './<%= fileName %>.schema';

@Injectable()
export class <%= className %>Repository extends BaseRepository<<%= className %>> {
  constructor(@InjectModel(<%= className %>.name) model: Model<<%= className %>>) {
    super(model);
  }
}
